<Window x:Class="SGuardLimiterMax.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="SGuard Limiter" Icon="/Assets/SGuard_Limiter_Max.png"
        Height="740" Width="960" MinWidth="820" MinHeight="620"
        WindowStyle="None" AllowsTransparency="False"
        Background="{DynamicResource PageBackground}"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>

        <!-- ═══ Subtle dot-grid background texture ═══ -->
        <DrawingBrush x:Key="BgTexture" Viewport="0,0,48,48" ViewportUnits="Absolute"
                      TileMode="Tile" Opacity="0.025">
            <DrawingBrush.Drawing>
                <GeometryDrawing Brush="{DynamicResource TextPrimary}">
                    <GeometryDrawing.Geometry>
                        <EllipseGeometry Center="24,24" RadiusX="0.6" RadiusY="0.6"/>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingBrush.Drawing>
        </DrawingBrush>

        <!-- ═══ Section Label ═══ -->
        <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="FontWeight" Value="Regular"/>
            <Setter Property="Foreground" Value="{DynamicResource SectionLabel}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- ═══ Square Checkbox (main items, 16px) ═══ -->
        <Style x:Key="SquareCheck" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Chk" Width="16" Height="16"
                                    Background="Transparent"
                                    BorderBrush="{DynamicResource CheckBorder}"
                                    BorderThickness="1.5" Margin="0,0,12,0"
                                    VerticalAlignment="Center"/>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Chk" Property="Background" Value="{DynamicResource CheckFill}"/>
                                <Setter TargetName="Chk" Property="BorderBrush" Value="{DynamicResource CheckFill}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chk" Property="BorderBrush"
                                        Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══ Small Square Checkbox (runtime items, 14px) ═══ -->
        <Style x:Key="SmallSquareCheck" TargetType="CheckBox">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Chk" Width="14" Height="14"
                                    Background="Transparent"
                                    BorderBrush="{DynamicResource CheckBorder}"
                                    BorderThickness="1.5" Margin="0,0,10,0"
                                    VerticalAlignment="Center"/>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Chk" Property="Background" Value="{DynamicResource CheckFill}"/>
                                <Setter TargetName="Chk" Property="BorderBrush" Value="{DynamicResource CheckFill}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chk" Property="BorderBrush"
                                        Value="{DynamicResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══ Inline Selector (Power Plan / Timer) ═══ -->
        <Style x:Key="InlineCombo" TargetType="ComboBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Background="Transparent" BorderThickness="0"
                                    BorderBrush="Transparent">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" VerticalAlignment="Center"
                                               Text="{TemplateBinding SelectionBoxItem}"
                                               TextDecorations="Underline"
                                               Foreground="{DynamicResource TextSecondary}"/>
                                    <TextBlock Grid.Column="1" Text="▾" FontSize="9"
                                               Foreground="{DynamicResource TextSecondary}"
                                               VerticalAlignment="Center" Margin="2,0,0,0"/>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide" Placement="Bottom">
                                <Border Background="{DynamicResource PageBackground}"
                                        BorderBrush="{DynamicResource DividerColor}"
                                        BorderThickness="1" MinWidth="160" MaxHeight="200">
                                    <ScrollViewer><ItemsPresenter/></ScrollViewer>
                                </Border>
                            </Popup>
                            <ToggleButton Background="Transparent" BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="FontFamily" Value="Segoe UI"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{DynamicResource TagBackground}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══ Text Link (apply / save / change) ═══ -->
        <Style x:Key="TextLink" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}"
                                   Foreground="{TemplateBinding Foreground}"
                                   VerticalAlignment="Center"
                                   TextDecorations="Underline"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══ ARM Button (border-only, in left bar) ═══ -->
        <Style x:Key="ArmButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource LeftBarForeground}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent"
                                BorderBrush="{DynamicResource ArmBorderOpacity}"
                                BorderThickness="1.5" Padding="12,8">
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush"
                                        Value="{DynamicResource ArmBorder}"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══ Title bar button ═══ -->
        <Style x:Key="TitleBarBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource LeftBarForeground}"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent" Opacity="0.3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ═══ Dark TextBox (underline style) ═══ -->
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource InputUnderline}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Padding" Value="4,4"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Bd" Background="Transparent"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush"
                                        Value="{DynamicResource InputUnderlineFocus}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <!-- ════════════════════════════════════════════════════════════════════ -->
    <!-- ROOT LAYOUT                                                         -->
    <!-- ════════════════════════════════════════════════════════════════════ -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="190"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ═══ LEFT BLACK BAR ═══ -->
        <Border Grid.Column="0" Background="{DynamicResource LeftBarBackground}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Brand + title bar buttons (drag handle) -->
                <Grid Grid.Row="0" Margin="16,22,12,0"
                      MouseLeftButtonDown="Window_MouseLeftButtonDown">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock FontFamily="Georgia" FontSize="24" LineHeight="22"
                                   Foreground="{DynamicResource LeftBarForeground}">
                            SGUARD<LineBreak/>LIMITER
                        </TextBlock>
                        <Border Width="20" Height="2" Margin="0,12,0,0"
                                Background="{DynamicResource LeftBarForeground}"
                                Opacity="0.3"/>
                        <TextBlock FontFamily="Segoe UI" FontSize="8"
                                   Foreground="{DynamicResource LeftBarForeground}"
                                   Opacity="0.45" Margin="0,6,0,0">
                            PERFORMANCE<LineBreak/>优化工具 v1.1
                        </TextBlock>
                    </StackPanel>

                    <!-- Title bar buttons -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal"
                                VerticalAlignment="Top">
                        <Button Style="{StaticResource TitleBarBtn}" Content="—"
                                Click="BtnMinimize_Click"/>
                        <Button Style="{StaticResource TitleBarBtn}" Content="✕"
                                Click="BtnClose_Click"/>
                    </StackPanel>
                </Grid>

                <!-- Status -->
                <StackPanel Grid.Row="1" Margin="16,28,16,0">
                    <TextBlock FontFamily="Segoe UI" FontSize="8"
                               Foreground="{DynamicResource LeftBarForeground}"
                               Opacity="0.35" Margin="0,0,0,4">
                        状态
                    </TextBlock>
                    <Ellipse Width="7" Height="7" Margin="0,0,0,8"
                             RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <ScaleTransform x:Name="DotScale" ScaleX="1" ScaleY="1"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="{DynamicResource StatusDotIdle}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsGameRunning}" Value="True">
                                        <Setter Property="Fill" Value="{DynamicResource StatusDotActive}"/>
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                                     To="1.6" Duration="0:0:0.7"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                                     To="1.6" Duration="0:0:0.7"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                                                     To="1" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                                                     To="1" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock FontFamily="Segoe UI" FontSize="13"
                               Foreground="{DynamicResource LeftBarForeground}"
                               Opacity="0.5">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="待机"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsGameRunning}" Value="True">
                                        <Setter Property="Text" Value="运行中"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <!-- ARM + Save -->
                <StackPanel Grid.Row="2" VerticalAlignment="Bottom" Margin="16,0,16,0">
                    <Button Style="{StaticResource ArmButton}" Content="启动"
                            Click="BtnEnterMonitor_Click" Margin="0,0,0,8"/>
                    <Button Content="保存设置" Click="BtnSaveConfig_Click" Margin="0,0,0,4">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Foreground" Value="{DynamicResource LeftBarForeground}"/>
                                <Setter Property="FontFamily" Value="Segoe UI"/>
                                <Setter Property="FontSize" Value="8"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <ContentPresenter x:Name="Cp" Opacity="0.3"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Cp" Property="Opacity" Value="0.7"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Theme toggle + Carved mark -->
                <StackPanel Grid.Row="3" Margin="0,0,0,14">
                    <Button Content="◐" Click="BtnToggleTheme_Click"
                            HorizontalAlignment="Center" Margin="0,0,0,10"
                            FontSize="16"
                            Foreground="{DynamicResource LeftBarForeground}"
                            Background="Transparent" BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="{DynamicResource LeftBarForeground}"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <ContentPresenter x:Name="Cp" Opacity="0.4"
                                                              HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Cp" Property="Opacity" Value="0.8"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                    <Grid>
                        <TextBlock FontFamily="Georgia" FontSize="15"
                                   Foreground="{DynamicResource CarvedDark}"
                                   Text="Nº 001" HorizontalAlignment="Center"
                                   Margin="0,1,0,0"/>
                        <TextBlock FontFamily="Georgia" FontSize="15"
                                   Foreground="{DynamicResource CarvedLight}"
                                   Text="Nº 001" HorizontalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ═══ RIGHT CONTENT ═══ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Subtle background texture -->
            <Rectangle Grid.Row="0" Grid.RowSpan="2" Fill="{StaticResource BgTexture}"
                       IsHitTestVisible="False"/>

            <!-- Drag handle strip at top of right content -->
            <Border Grid.Row="0" Height="28"
                    MouseLeftButtonDown="Window_MouseLeftButtonDown"
                    Background="Transparent">
                <!-- Subtle decorative rule -->
                <Border BorderBrush="{DynamicResource DividerColor}"
                        BorderThickness="0,0,0,1" Opacity="0.4"
                        VerticalAlignment="Bottom" Margin="28,0,28,0"/>
            </Border>

            <!-- Main content -->
            <Border Grid.Row="1" Padding="28,0,28,24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- OPTIMIZATIONS section header -->
                    <TextBlock Grid.Row="0" Style="{StaticResource SectionLabelStyle}"
                               Text="优化选项"/>

                    <!-- OPTIMIZATIONS items -->
                    <StackPanel Grid.Row="1">
                        <!-- Throttle SGuard -->
                        <Border BorderBrush="{DynamicResource DividerColor}"
                                BorderThickness="0,0,0,1" Padding="0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" Style="{StaticResource SquareCheck}"
                                          IsChecked="{Binding ThrottleSGuard}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="13"
                                           Foreground="{DynamicResource TextPrimary}"
                                           Text="限制 SGuard"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="10"
                                           Foreground="{DynamicResource TextMuted}"
                                           Text="空闲 · 末核"/>
                            </Grid>
                        </Border>

                        <!-- Boost Game Priority -->
                        <Border BorderBrush="{DynamicResource DividerColor}"
                                BorderThickness="0,0,0,1" Padding="0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" Style="{StaticResource SquareCheck}"
                                          IsChecked="{Binding BoostGamePriority}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="13"
                                           Foreground="{DynamicResource TextPrimary}"
                                           Text="提升游戏优先级"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="10"
                                           Foreground="{DynamicResource TextMuted}"
                                           Text="高优先级"/>
                            </Grid>
                        </Border>

                        <!-- Unbind CPU 0 -->
                        <Border BorderBrush="{DynamicResource DividerColor}"
                                BorderThickness="0,0,0,1" Padding="0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" Style="{StaticResource SquareCheck}"
                                          IsChecked="{Binding UnbindCPU}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="13"
                                           Foreground="{DynamicResource TextPrimary}"
                                           Text="解绑 CPU 0"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="10"
                                           Foreground="{DynamicResource TextMuted}"
                                           Text="关闭"/>
                            </Grid>
                        </Border>

                        <!-- Power Plan with inline selector -->
                        <Border BorderBrush="{DynamicResource DividerColor}"
                                BorderThickness="0,0,0,1" Padding="0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" Style="{StaticResource SquareCheck}"
                                          IsChecked="{Binding OptimizePower}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="13"
                                           Foreground="{DynamicResource TextPrimary}"
                                           Text="电源计划"/>
                                <ComboBox Grid.Column="2" Style="{StaticResource InlineCombo}"
                                          ItemsSource="{Binding AvailablePowerPlans}"
                                          SelectedItem="{Binding SelectedTargetPlan, Mode=TwoWay}"
                                          DisplayMemberPath="Name" Margin="6,0,0,0"
                                          ToolTip="检测到游戏时切换到此计划"/>
                                <Button Grid.Column="3" Content="应用"
                                        Style="{StaticResource TextLink}"
                                        Click="BtnApplyPlan_Click" Margin="12,0,0,0"/>
                                <Button Grid.Column="4" Content="刷新"
                                        Style="{StaticResource TextLink}"
                                        Click="BtnRefreshPlans_Click" Margin="6,0,0,0"/>
                            </Grid>
                        </Border>

                        <!-- Flush DNS -->
                        <Border BorderBrush="{DynamicResource DividerColor}"
                                BorderThickness="0,0,0,1" Padding="0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" Style="{StaticResource SquareCheck}"
                                          IsChecked="{Binding FlushDNS}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="13"
                                           Foreground="{DynamicResource TextPrimary}"
                                           Text="刷新 DNS"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="10"
                                           Foreground="{DynamicResource TextMuted}"
                                           Text="检测时"/>
                            </Grid>
                        </Border>

                        <!-- Timer Resolution with inline selector -->
                        <Border Padding="0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0" Style="{StaticResource SquareCheck}"
                                          IsChecked="{Binding TimerResolution}"/>
                                <TextBlock Grid.Column="1" VerticalAlignment="Center"
                                           FontFamily="Segoe UI" FontSize="13"
                                           Foreground="{DynamicResource TextPrimary}"
                                           Text="计时器精度"/>
                                <ComboBox Grid.Column="2" Style="{StaticResource InlineCombo}"
                                          ItemsSource="{Binding TimerResolutionOptions}"
                                          SelectedItem="{Binding SelectedTimerResolutionOption, Mode=TwoWay}"
                                          DisplayMemberPath="Label" Margin="6,0,0,0"
                                          ToolTip="检测到游戏时应用，退出时恢复"/>
                                <Button Grid.Column="3" Content="应用"
                                        Style="{StaticResource TextLink}"
                                        Click="BtnApplyTimer_Click" Margin="12,0,0,0"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- CUSTOM GAMES section -->
                    <StackPanel Grid.Row="2" Margin="0,18,0,0">
                        <TextBlock Style="{StaticResource SectionLabelStyle}"
                                   Text="自定义游戏"/>

                        <!-- Game tags -->
                        <ItemsControl ItemsSource="{Binding CustomGames}" Margin="0,0,0,8">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource TagBackground}"
                                            Padding="8,4" Margin="0,0,6,6">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center"
                                                       FontFamily="Segoe UI" FontSize="11"
                                                       Foreground="{DynamicResource TextPrimary}"/>
                                            <!-- Priority dot -->
                                            <Border Width="8" Height="8" Margin="6,0,0,0"
                                                    VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{DynamicResource CheckFill}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding BoostPriority}" Value="False">
                                                                <Setter Property="Background" Value="Transparent"/>
                                                                <Setter Property="BorderBrush" Value="{DynamicResource CheckBorder}"/>
                                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                            <!-- CPU0 dot -->
                                            <Border Width="8" Height="8" Margin="3,0,0,0"
                                                    VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{DynamicResource CheckFill}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding UnbindCpu0}" Value="False">
                                                                <Setter Property="Background" Value="Transparent"/>
                                                                <Setter Property="BorderBrush" Value="{DynamicResource CheckBorder}"/>
                                                                <Setter Property="BorderThickness" Value="1.5"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                            </Border>
                                            <!-- Remove -->
                                            <Button Content="×" Tag="{Binding}" Click="BtnRemoveGame_Click"
                                                    Margin="6,0,0,0" VerticalAlignment="Center"
                                                    FontFamily="Segoe UI" FontSize="13"
                                                    Foreground="{DynamicResource TextMuted}"
                                                    Background="Transparent" BorderThickness="0"
                                                    Cursor="Hand" Padding="2,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Empty state -->
                        <TextBlock FontFamily="Segoe UI" FontSize="11"
                                   Foreground="{DynamicResource TextDisabled}"
                                   Margin="0,0,0,8" Text="暂无自定义进程，请在下方添加。">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CustomGames.Count}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <!-- Add game form (inline) -->
                        <StackPanel Orientation="Horizontal">
                            <TextBox x:Name="TxtProcessName" Style="{StaticResource DarkTextBox}"
                                     Width="120" Margin="0,0,6,0"
                                     Tag="ProcessName"/>
                            <TextBox x:Name="TxtDisplayName" Style="{StaticResource DarkTextBox}"
                                     Width="100" Margin="0,0,8,0"
                                     Tag="DisplayName"/>
                            <CheckBox x:Name="ChkBoostPriority" IsChecked="True"
                                      VerticalAlignment="Center" Margin="0,0,4,0">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="FontFamily" Value="Segoe UI"/>
                                        <Setter Property="FontSize" Value="10"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>
                            <TextBlock Text="高优先" FontFamily="Segoe UI" FontSize="10"
                                       Foreground="{DynamicResource TextSecondary}"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <CheckBox x:Name="ChkUnbindCpu0"
                                      VerticalAlignment="Center" Margin="0,0,4,0">
                                <CheckBox.Style>
                                    <Style TargetType="CheckBox">
                                        <Setter Property="FontFamily" Value="Segoe UI"/>
                                        <Setter Property="FontSize" Value="10"/>
                                        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                    </Style>
                                </CheckBox.Style>
                            </CheckBox>
                            <TextBlock Text="CPU0" FontFamily="Segoe UI" FontSize="10"
                                       Foreground="{DynamicResource TextSecondary}"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Button Content="添加" Style="{StaticResource TextLink}"
                                    Click="BtnAddGame_Click" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- RUNTIME section -->
                    <StackPanel Grid.Row="3" Margin="0,18,0,0" VerticalAlignment="Top">
                        <TextBlock Style="{StaticResource SectionLabelStyle}"
                                   Text="运行时"/>

                        <UniformGrid Columns="2" Rows="3">
                            <CheckBox Style="{StaticResource SmallSquareCheck}"
                                      IsChecked="{Binding AutoMinimizeOnGame}"
                                      Content="启动后最小化到托盘"/>
                            <CheckBox Style="{StaticResource SmallSquareCheck}"
                                      IsChecked="{Binding ExitWithGame}"
                                      Content="游戏退出时关闭"/>
                            <CheckBox Style="{StaticResource SmallSquareCheck}"
                                      IsChecked="{Binding AutoStart}"
                                      Content="开机自启"/>
                            <CheckBox Style="{StaticResource SmallSquareCheck}"
                                      IsChecked="{Binding RestorePowerOnExit}"
                                      Content="退出时恢复电源计划"/>
                            <CheckBox Style="{StaticResource SmallSquareCheck}"
                                      IsChecked="{Binding RestoreTimerOnExit}"
                                      Content="退出时恢复计时器"/>
                            <CheckBox Style="{StaticResource SmallSquareCheck}"
                                      IsChecked="{Binding ShowNotifications}"
                                      Content="气泡通知"/>
                        </UniformGrid>
                    </StackPanel>

                    <!-- Bottom status zone -->
                    <Border Grid.Row="4" BorderBrush="{DynamicResource DividerColor}"
                            BorderThickness="0,1,0,0" Padding="0,10,0,0"
                            Margin="0,12,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Giant status text -->
                            <StackPanel Grid.Column="0" VerticalAlignment="Bottom">
                                <TextBlock FontFamily="Georgia" FontSize="48" FontWeight="Light"
                                           LineHeight="42"
                                           Foreground="{DynamicResource StatusGiantText}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="待机"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsGameRunning}" Value="True">
                                                    <Setter Property="Text" Value="运行中"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock FontFamily="Segoe UI" FontSize="10"
                                           Foreground="{DynamicResource TextMuted}"
                                           Text="{Binding StatusText}"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>

                            <!-- Apply Now -->
                            <Button Grid.Column="1" VerticalAlignment="Bottom"
                                    Content="立即应用" Click="BtnApplyNow_Click"
                                    FontFamily="Segoe UI" FontSize="12"
                                    Foreground="{DynamicResource TextPrimary}"
                                    Background="Transparent" BorderBrush="{DynamicResource TextPrimary}"
                                    BorderThickness="1.5" Padding="18,10"
                                    Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Bd" Background="Transparent"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background"
                                                        Value="{DynamicResource TextPrimary}"/>
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource PageBackground}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
